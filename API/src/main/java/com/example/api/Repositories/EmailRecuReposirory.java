package com.example.api.Repositories;

import com.example.api.entities.EmailRecu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmailRecuReposirory extends JpaRepository<EmailRecu, Integer> {
}
